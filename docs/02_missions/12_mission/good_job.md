# Good Job !!!!

121301
第１２ミッション、いかがだっただろうか。

私は、みなさんがミッションを見事クリアしてくれたと信じている。

ここで、このミッションで学んだことをまとめてみたいと思う。

初めに、ABC順に並べることについて振り返る。

* ABC順に並び替えることを昇順にソートするという
* ソートするにはリストのsortメソッドを使う

次に、リストを結合した文字列を作ることについて振り返る。

* リストを結合した文字列を作るためには、joinメソッドを使う
* 間に挟む文字列のjoinメソッドとする
* joinメソッドでは、引数に結合対象のリストを指定する
* joinメソッドは、結合された文字列が戻り値で返却される

最後に、ファイルを書き込むことについて振り返る。

* ファイルオブジェクトのwriteメソッドを使ってファイルを書き込む
* writeメソッドの引数で書き込みたい文字列を指定する

以上が、このミッションで学んだことだ。

ここで学んだスキルは、これからさらに挑むであろうPythonの学習で、君たちを勇気付けてくれるスキルだと、信じている。

以上で、最終ミッションのセクションを終了する。