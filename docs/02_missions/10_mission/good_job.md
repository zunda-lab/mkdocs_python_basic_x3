# Good Job !!!!

第１０ミッション、いかがだっただろうか。

私は、みなさんがミッションを見事クリアしてくれたと信じている。

ここで、このミッションで学んだことをまとめてみたいと思う。

初めに、関数について振り返る。

* 関数とは一連の処理の集まり
* 呼び出すことで処理をまとめて実行してくれる
* 複数の文をまとめて名前をつけたもの
* 組み込み関数とオリジナルの関数がある

次に、オリジナルの関数について振り返る。

* 関数を定義してから関数を呼び出す
* オリジナルの関数はdef文で定義する

次は、引数と戻り値について振り返る。

* 引数とは関数が仕事をするために必要なデータを送り込むもの
* 戻り値とは関数から呼び出し元へデータを渡すもの
* return文で戻り値を返す

最後に、繰り返し文字列の生成について振り返る。

* 「繰り返したい文字列・アスタリスク・繰り返したい回数」と書くと、繰り返された新しい文字列を取得することができる

以上が、このミッションで学んだことだ。ここで学んだスキルは、これから挑むであろうミッションで、君たちを救ってくれるスキルだと、信じている。以上で、第１０ミッションのセクションを終了する。